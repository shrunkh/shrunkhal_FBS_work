#include <stdio.h>

int main() {
    float radius = 10;   
    float area;

    area = 3.14 * radius * radius;

    printf("Radius = %f\n", radius);
    printf("Area of Circle = %f\n", area);

    return 0;
}
